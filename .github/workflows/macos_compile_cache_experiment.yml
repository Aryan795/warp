on:
  pull_request:
    branches:
      - master
    types:
      - opened
      - reopened
      - synchronize

name: macOS Compile Cache Experiment
concurrency:
  group: macos-compile-cache-experiment-treatment-volumes
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  NEXTEST_PROFILE: ci
  WORKSPACE_TEST_ARGS: --workspace --locked --exclude command-signatures-v2
  RUSTFLAGS: -C debuginfo=line-tables-only --cfg=web_sys_unstable_apis

jobs:
  parameters:
    name: Resolve experiment phase
    if: >-
      github.repository_owner == 'warpdotdev' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'kevinyang/macos-ci-observability'
    timeout-minutes: 3
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      block: ${{ steps.resolve.outputs.block }}
      phase: ${{ steps.resolve.outputs.phase }}
      seed_attempt: ${{ steps.resolve.outputs.seed_attempt }}
    steps:
      - name: Resolve alternating seed and measure attempts
        id: resolve
        env:
          MERGE_SHA: ${{ github.sha }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
          SOURCE_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if (( RUN_ATTEMPT % 2 == 1 )); then
            phase=seed
            seed_attempt="$RUN_ATTEMPT"
          else
            phase=measure
            seed_attempt="$((RUN_ATTEMPT - 1))"
          fi
          block="$(((RUN_ATTEMPT + 1) / 2))"
          {
            printf 'block=%s\n' "$block"
            printf 'phase=%s\n' "$phase"
            printf 'seed_attempt=%s\n' "$seed_attempt"
          } >> "$GITHUB_OUTPUT"
          {
            echo "Block: $block"
            echo "Phase: $phase"
            echo "Seed attempt: $seed_attempt"
            echo "Merge SHA: $MERGE_SHA"
            echo "Source SHA: $SOURCE_SHA"
          } >> "$GITHUB_STEP_SUMMARY"

  seed:
    name: Seed block ${{ needs.parameters.outputs.block }} (${{ matrix.cache_scope }})
    needs: parameters
    if: needs.parameters.outputs.phase == 'seed'
    timeout-minutes: 40
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - cache_scope: 50gb
            runner: namespace-profile-mac-ci-tests-50gb
          - cache_scope: 84gb
            runner: namespace-profile-mac-ci-tests-84gb
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: ${{ github.sha }}

      - uses: ./.github/actions/prepare_environment
        id: prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          install_test_deps: true

      - name: Install cargo nextest
        uses: taiki-e/install-action@16b05812d776ae1dfaabc8277e421fb6d2506419 # v2.82.7
        with:
          tool: nextest

      - name: Seed cache
        uses: ./.github/actions/compile_macos_tests
        with:
          workspace_test_args: ${{ env.WORKSPACE_TEST_ARGS }}
          source_sha: ${{ github.event.pull_request.head.sha }}
          runner_profile: ${{ matrix.runner }}
          cache_scope: ${{ matrix.cache_scope }}
          counted: 'false'
          cache_provider: ${{ steps.prepare_environment.outputs.rust_cache_provider }}
          cache_identity: ${{ steps.prepare_environment.outputs.rust_cache_identity }}
          cache_hit: ${{ steps.prepare_environment.outputs.rust_cache_hit }}
          artifact_name: macos-compile-cache-${{ matrix.cache_scope }}-seed-${{ github.run_id }}-${{ github.run_attempt }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  validate_seed:
    name: Validate block ${{ needs.parameters.outputs.block }} seed
    needs:
      - parameters
      - seed
    if: needs.parameters.outputs.phase == 'seed' && needs.seed.result == 'success'
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Download 50 GB seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-50gb-seed-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/seed-50gb

      - name: Download 84 GB seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-84gb-seed-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/seed-84gb

      - name: Compare treatment seed inputs
        shell: bash
        run: |
          set -euo pipefail
          for key in checked_out_tree_sha environment_identity_sha; do
            value_50gb="$(sed -n "s/^$key=//p" "$RUNNER_TEMP/seed-50gb/metrics.env")"
            value_84gb="$(sed -n "s/^$key=//p" "$RUNNER_TEMP/seed-84gb/metrics.env")"
            if [ -z "$value_50gb" ] || [ "$value_50gb" != "$value_84gb" ]; then
              echo "::error::Treatment seeds do not share $key"
              exit 1
            fi
          done

  validate_measurement:
    name: Load block ${{ needs.parameters.outputs.block }} seed
    needs: parameters
    if: needs.parameters.outputs.phase == 'measure'
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Download 50 GB seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-50gb-seed-${{ github.run_id }}-${{ needs.parameters.outputs.seed_attempt }}
          path: ${{ runner.temp }}/seed-50gb

      - name: Download 84 GB seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-84gb-seed-${{ github.run_id }}-${{ needs.parameters.outputs.seed_attempt }}
          path: ${{ runner.temp }}/seed-84gb

      - name: Validate prior-attempt seed artifacts
        shell: bash
        run: |
          set -euo pipefail
          for key in checked_out_tree_sha environment_identity_sha; do
            value_50gb="$(sed -n "s/^$key=//p" "$RUNNER_TEMP/seed-50gb/metrics.env")"
            value_84gb="$(sed -n "s/^$key=//p" "$RUNNER_TEMP/seed-84gb/metrics.env")"
            if [ -z "$value_50gb" ] || [ "$value_50gb" != "$value_84gb" ]; then
              echo "::error::Prior-attempt seeds do not share $key"
              exit 1
            fi
          done

  measure:
    name: Measure block ${{ needs.parameters.outputs.block }} (${{ matrix.cache_scope }})
    needs:
      - parameters
      - validate_measurement
    if: needs.parameters.outputs.phase == 'measure' && needs.validate_measurement.result == 'success'
    timeout-minutes: 40
    runs-on: ${{ matrix.runner }}
    permissions:
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - cache_scope: 50gb
            runner: namespace-profile-mac-ci-tests-50gb
          - cache_scope: 84gb
            runner: namespace-profile-mac-ci-tests-84gb
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: ${{ github.sha }}

      - name: Download seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-${{ matrix.cache_scope }}-seed-${{ github.run_id }}-${{ needs.parameters.outputs.seed_attempt }}
          path: ${{ runner.temp }}/seed-diagnostics

      - name: Read seed identity
        id: seed_identity
        shell: bash
        run: |
          set -euo pipefail
          metrics="$RUNNER_TEMP/seed-diagnostics/metrics.env"
          for key in checked_out_tree_sha environment_identity_sha post_target_manifest_sha post_target_kib; do
            value="$(sed -n "s/^$key=//p" "$metrics")"
            if [ -z "$value" ]; then
              echo "::error::Seed diagnostics are missing $key"
              exit 1
            fi
            printf '%s=%s\n' "$key" "$value" >> "$GITHUB_OUTPUT"
          done

      - uses: ./.github/actions/prepare_environment
        id: prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          install_test_deps: true

      - name: Install cargo nextest
        uses: taiki-e/install-action@16b05812d776ae1dfaabc8277e421fb6d2506419 # v2.82.7
        with:
          tool: nextest

      - name: Measure restored cache
        uses: ./.github/actions/compile_macos_tests
        with:
          workspace_test_args: ${{ env.WORKSPACE_TEST_ARGS }}
          source_sha: ${{ github.event.pull_request.head.sha }}
          runner_profile: ${{ matrix.runner }}
          cache_scope: ${{ matrix.cache_scope }}
          counted: 'true'
          expected_tree_sha: ${{ steps.seed_identity.outputs.checked_out_tree_sha }}
          expected_environment_sha: ${{ steps.seed_identity.outputs.environment_identity_sha }}
          expected_target_manifest_sha: ${{ steps.seed_identity.outputs.post_target_manifest_sha }}
          expected_target_kib: ${{ steps.seed_identity.outputs.post_target_kib }}
          cache_provider: ${{ steps.prepare_environment.outputs.rust_cache_provider }}
          cache_identity: ${{ steps.prepare_environment.outputs.rust_cache_identity }}
          cache_hit: ${{ steps.prepare_environment.outputs.rust_cache_hit }}
          artifact_name: macos-compile-cache-${{ matrix.cache_scope }}-measure-${{ github.run_id }}-${{ github.run_attempt }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  scratch:
    name: Measure block ${{ needs.parameters.outputs.block }} scratch
    needs:
      - parameters
      - validate_measurement
    if: needs.parameters.outputs.phase == 'measure' && needs.validate_measurement.result == 'success'
    timeout-minutes: 40
    runs-on: namespace-profile-mac-ci-tests-scratch
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: ${{ github.sha }}

      - name: Download matched seed diagnostics
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: macos-compile-cache-50gb-seed-${{ github.run_id }}-${{ needs.parameters.outputs.seed_attempt }}
          path: ${{ runner.temp }}/seed-diagnostics

      - name: Read matched build identity
        id: seed_identity
        shell: bash
        run: |
          set -euo pipefail
          metrics="$RUNNER_TEMP/seed-diagnostics/metrics.env"
          for key in checked_out_tree_sha environment_identity_sha; do
            value="$(sed -n "s/^$key=//p" "$metrics")"
            if [ -z "$value" ]; then
              echo "::error::Seed diagnostics are missing $key"
              exit 1
            fi
            printf '%s=%s\n' "$key" "$value" >> "$GITHUB_OUTPUT"
          done

      - name: Use ephemeral Rust paths
        run: |
          {
            echo "CARGO_HOME=$RUNNER_TEMP/cargo-home"
            echo "CARGO_TARGET_DIR=$RUNNER_TEMP/cargo-target"
          } >> "$GITHUB_ENV"

      - uses: ./.github/actions/prepare_environment
        id: prepare_environment
        with:
          target_os: macos
          is_self_hosted: false
          install_test_deps: true
          enable_rust_cache: false
          enable_namespace_cache: false

      - name: Install cargo nextest
        uses: taiki-e/install-action@16b05812d776ae1dfaabc8277e421fb6d2506419 # v2.82.7
        with:
          tool: nextest

      - name: Measure scratch compile
        uses: ./.github/actions/compile_macos_tests
        with:
          workspace_test_args: ${{ env.WORKSPACE_TEST_ARGS }}
          source_sha: ${{ github.event.pull_request.head.sha }}
          runner_profile: namespace-profile-mac-ci-tests-scratch
          cache_scope: scratch
          counted: 'true'
          expected_tree_sha: ${{ steps.seed_identity.outputs.checked_out_tree_sha }}
          expected_environment_sha: ${{ steps.seed_identity.outputs.environment_identity_sha }}
          cache_provider: ${{ steps.prepare_environment.outputs.rust_cache_provider }}
          cache_identity: ${{ steps.prepare_environment.outputs.rust_cache_identity }}
          cache_hit: ${{ steps.prepare_environment.outputs.rust_cache_hit }}
          artifact_name: macos-compile-cache-scratch-${{ github.run_id }}-${{ github.run_attempt }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
